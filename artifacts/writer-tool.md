# Writer tool — notes that are not bible rules

Out-of-scope item one. Wanted, not built. The renderer reads and never
writes. If moving a thing upward commits it, this tool is what writes.

The commit gesture is also filed in `DESIGN-BIBLE.md` under a
deliberate-misfiling note. Do not resolve that. Do not relocate it
quietly.

## Auth for commits on a persona's behalf

Parked. Owner leans toward API keys over per-persona git logins, on
the grounds that keys are cleaner and can be burned if shared.
Writer-tool concern, not bible material. Recorded so it is not lost.

## File handoff

See `artifacts/break-to-build.md` item 5.
